services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - go-api
    cap_add:
      - NET_BIND_SERVICE
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./secrets/ca.pem:/etc/ssl/certs/ca.pem
      - ./secrets/web.crt:/etc/ssl/certs/crt.pem
      - ./secrets/web.key:/etc/ssl/certs/key.pem
    networks:
      - backend
      - keycloak

  keycloak:
    image: quay.io/keycloak/keycloak:26.2
    container_name: keycloak
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_HEALTH_ENABLED: "true"
    command: start-dev --import-realm
    ports:
      - 8081:8080
    volumes:
      - ./keycloak/imports:/opt/keycloak/data/import
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/9000;echo -e 'GET /health/ready HTTP/1.1\r\nhost: http://localhost\r\nConnection: close\r\n\r\n' >&3;if [ $? -eq 0 ]; then echo 'Healthcheck Successful';exit 0;else echo 'Healthcheck Failed';exit 1;fi;"]
      interval: 5s
      timeout: 5s
      retries: 30
    networks:
      - keycloak
      - backend

  # Go API Service
  go-api:
    build:
      context: ./server
      dockerfile: Dockerfile
    user: "${UID}:${GID}"
    restart: always
    container_name: go-api
    image: lf4a/mqtt-ss-api:local
    env_file: ./server/.env
    depends_on:
      mongo-db:
        condition: service_started
      broker:
        condition: service_started
      keycloak:
        condition: service_healthy
    ports:
      - 5000:5000
    networks:
      backend:
        aliases:
          - api.lf4a.com
      db:
      minio:
      broker:
    volumes:
      - ./secrets/ca.pem:/certs/ca.crt
      - ./secrets/server.crt:/certs/server.crt
      - ./secrets/server.key:/certs/server.key
      - ./secrets/client.crt:/certs/client.crt
      - ./secrets/client.key:/certs/client.key

  broker:
    user: "${UID}:${GID}"
    image: eclipse-mosquitto:latest
    container_name: broker
    hostname: broker
    ports:
      - 8883:8883
    volumes:
      - ./broker/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - ./secrets/ca.pem:/certs/ca.crt
      - ./secrets/broker.crt:/certs/server.crt
      - ./secrets/broker.key:/certs/server.key
    networks:
      - broker

  mongo-db:
    container_name: mongo-db
    restart: always
    image: mongo:latest
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:-root}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-password}
    command: ["--auth"]
    volumes:
      - mongo-data:/data/db
    networks:
      - db
  
  mongo-express:
    container_name: mongo-express
    restart: always
    image: mongo-express:latest
    environment:
      ME_CONFIG_BASICAUTH_USERNAME: ${MONGO_EXPRESS_USER:-admin}
      ME_CONFIG_BASICAUTH_PASSWORD: ${MONGO_EXPRESS_PASSWORD:-password}
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGO_INITDB_ROOT_USERNAME:-root}
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-password}
      ME_CONFIG_MONGODB_SERVER: "mongo-db"
      ME_CONFIG_SITE_SSL_ENABLED: "true"
      ME_CONFIG_SITE_SSL_CRT_PATH: "/certs/express.crt"
      ME_CONFIG_SITE_SSL_KEY_PATH: "/certs/express.key"
    ports:
      - 8082:8081
    depends_on:
      - mongo-db
    networks:
      - db
    volumes:
      - ./secrets/express.crt:/certs/express.crt
      - ./secrets/express.key:/certs/express.key
  
  minio:
    container_name: minio
    restart: always
    image: quay.io/minio/minio:latest
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    command: server /data --certs-dir /certs --console-address ":9001"
    ports:
      - 9000:9000
      - 9001:9001
    volumes:
      - minio-data:/data
      - ./secrets/ca.pem:/certs/ca.crt
      - ./secrets/minio.crt:/certs/public.crt
      - ./secrets/minio.key:/certs/private.key
    networks:
      minio:
        aliases:
          - minio.lf4a.com

volumes:
  mongo-data:
  minio-data:

networks:
  keycloak:
  backend:
  db:
  minio:
  broker:
